[package]
name = "fluidaudio-rs"
version = "0.14.1"
edition = "2021"
license = "MIT"
description = "Local-only FluidAudio diarization bridge for gcrdings"
build = "build.rs"

[features]
default = ["diarization"]
diarization = []

[lib]
name = "fluidaudio_rs"
path = "src/lib.rs"
