# Incorporate upstream updates selectively

gcrdings will retain Meetily Community as an upstream remote and review its releases, but it will not merge them automatically. Compatible fixes and improvements will enter as explicit, independently verified commits so upstream changes cannot silently alter gcrdings data, privacy, security, or user-facing contracts.
